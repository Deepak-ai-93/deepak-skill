# Author memory — the durable file every prompt-library build reads + writes

**File:** `prompt-memory.md` — lives at the **working folder root**, NOT inside the skill. Created at **Stage 0** on first run (taste capture, ≤ 3 questions + optional writing sample), read at the start of EVERY build, updated at the end (Stage 7).

## 1. Author identity

- Name / brand:
- What they do / their niche:
- Tools they use (Claude / ChatGPT / Gemini / producer skills…):

## 2. Taste profile — the author's voice (prompts + outputs must sound like THIS)

- Tone: (direct + confident / warm + plain / playful + punchy / calm + measured)
- Sentence rhythm: (short declarative / long flowing / mixed)
- Person + humor: (heavy "you" / first-person / none / light asides)
- Jargon: (plain words / industry terms, defined)
- Pet phrases (use sparingly): (e.g. "the real fix")
- **Banned words** (the audit checks these are carried into the library's voice rules — bullets under this exact heading):
  - word they never want to see

## 3. Past prompt-library builds (append one line per build — grows forever)

| Date | Use cases covered | What the author kept / dropped | Verdict (/50) |
|---|---|---|---|

## 4. Standing facts (never delete)

- Offer / CTA / links / handles:

## Memory rules

- **READ at Stage 0, WRITE at the end. Never skip either.**
- The taste section becomes the `## Voice rules` of the library — keep it current.
- Append to the past-builds table every time (what survived the test loop is the signal).
- Keep standing facts until the author changes them.
